# SalePage Mobile env (used by app.config.ts -> Constants.expoConfig.extra)
# Copy to .env.local for local dev. Production values are baked into EAS build
# profiles in eas.json so you don't need .env in CI.

# API base. Use http://<LAN-IP>:3000 when running web on simulator/device.
EXPO_PUBLIC_API_BASE_URL=https://salepage.in.th

# Platform-level LINE LIFF (single LIFF for all SalePage shops, see web app)
EXPO_PUBLIC_LINE_LIFF_ID=
EXPO_PUBLIC_LINE_LOGIN_CHANNEL_ID=

# Google OAuth client ID — same value as the web's AUTH_GOOGLE_ID env. Buyers
# who sign in via Google on either web or mobile land on the same User row
# (find/create by email). For Expo Go dev, add
# `https://auth.expo.io/@<owner>/<slug>` to the OAuth client's authorized
# redirect URIs; for EAS builds, add `salepage://auth/google`.
EXPO_PUBLIC_GOOGLE_OAUTH_CLIENT_ID=

# Sentry telemetry DSN — required for TestFlight per CET TELEMETRY FIRST law.
# Without it, init() is a no-op and native crashes are uninvestigatable. Make
# sure the DSN matches the project you actually create in Sentry; ship the same
# value in EAS production builds so symbolicated stacks attach to the right
# release.
EXPO_PUBLIC_SENTRY_DSN=

# EAS / Expo project linkage (set after `eas init`)
EAS_PROJECT_ID=
